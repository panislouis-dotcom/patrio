.vercel
.DS_Store
**/.DS_Store

# Herramientas de preview (Playwright + node_modules)
.preview-tools/
node_modules/
.playwright-mcp/
.superpowers/

# Documentos de empresa (socios, marca, presentaciones) y trabajo por proyecto:
# privados — NO se versionan aquí ni se publican en Vercel.
# Anclado a la raíz con "/" para NO ignorar assets/proyectos (fotos reales del sitio).
/Empresa/
/Proyectos/

# App (from refigan)
*.db
*.db-shm
*.db-wal
dist/
.vite/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
app/api/var/files/**
!app/api/var/files/.gitkeep
00Tree.html
.env
docs/superpowers/.coverage

# Árboles de trabajo aislados (git worktree): son checkouts, no fuente
.worktrees/

# El bundle del plano es un artefacto de build (make build-plano), no fuente:
# se genera desde app/web/src/lib/floorplan/planSheets.ts
app/web/dist-plano/
app/api/assets/plano.iife.js
